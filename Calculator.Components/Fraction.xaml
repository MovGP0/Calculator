<UserControl
    x:Class="Calculator.Components.Fraction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculator.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="1" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ContentPresenter Grid.Column="0" Grid.Row="0" Content="{Binding Numerator, ElementName=control}"/>
        <Border Grid.Column="0" Grid.Row="1" Height="1" Margin="2" Background="Black"/>
        <ContentPresenter Grid.Column="0" Grid.Row="2" Content="{Binding Denominator, ElementName=control}"/>
    </Grid>
</UserControl>
