<UserControl
    x:Class="Calculator.Controls.Fraction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculator.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" x:Name="This">
    <StackPanel DataContext="{Binding ElementName=This}">
        <ContentPresenter Content="{Binding Numerator}" HorizontalAlignment="Center" />
        <Border Height="2" Margin="2" Background="Black" />
        <ContentPresenter Content="{Binding Denominator}" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
