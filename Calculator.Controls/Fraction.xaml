<Control
    x:Class="Calculator.Controls.Fraction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculator.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Calculator.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" x:Name="This" Template="{DynamicResource FractionTemplate}">
    <Control.Resources>
        <ControlTemplate x:Key="FractionTemplate" TargetType="controls:Fraction">
            <AdornerDecorator>
                <Canvas DataContext="{Binding ElementName=This}">
                    <ContentPresenter Content="{Binding Numerator}" Canvas.Left="{Binding NumeratorLeft}" Canvas.Top="0" />
                    <Border Background="{Binding Foreground}" Canvas.Top="{Binding LineTop}" Canvas.Left="0" Height="{Binding LineHeight}" Width="{Binding LineWidth}" />
                    <ContentPresenter Content="{Binding Denominator}" HorizontalAlignment="Center" Canvas.Left="{Binding DenominatorLeft}" Canvas.Top="{Binding DenominatorTop}" />
                </Canvas>
            </AdornerDecorator>
        </ControlTemplate>
    </Control.Resources>
</Control>